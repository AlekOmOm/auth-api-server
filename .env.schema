# === AUTH-SYSTEM SCHEMA CONFIGURATION ===
# Generated by init-auth-schema.js on 2025-05-28T14:16:35.851Z
# 
# These credentials connect your app to its dedicated database schema
# in the Auth-System. Keep these values secure and persistent.

# === CLIENT CREDENTIALS ===
CLIENT_ID=client_fbbac14b71304afdb591a03ae3133e50
CLIENT_SECRET=8d499a99-33f2-4731-b57a-941aeebccbaf

# === DATABASE SCHEMA ===
ASSIGNED_SCHEMA_NAME=client_tradingsimulator_1748441795542

# === APPLICATION CONFIGURATION ===
APP_NAME=TradingSimulator

# === ALLOWED RETURN URLS ===
# URLs that Auth-System can redirect back to after authentication
ALLOWED_RETURN_URL_1=http://localhost:3000
ALLOWED_RETURN_URL_2=http://localhost:3000/
ALLOWED_RETURN_URL_3=http://localhost:3000/dashboard
ALLOWED_RETURN_URL_4=http://localhost:3000/profile
ALLOWED_RETURN_URL_5=http://localhost:3000/portfolio
ALLOWED_RETURN_URL_6=http://localhost:3000/trading
ALLOWED_RETURN_URL_7=http://localhost:3000/auth/callback
ALLOWED_RETURN_URL_8=http://localhost:3001
ALLOWED_RETURN_URL_9=http://localhost:3001/
ALLOWED_RETURN_URL_10=http://localhost:3001/dashboard
ALLOWED_RETURN_URL_11=http://localhost:3001/profile
ALLOWED_RETURN_URL_12=http://localhost:3001/portfolio
ALLOWED_RETURN_URL_13=http://localhost:3001/trading
ALLOWED_RETURN_URL_14=http://localhost:3001/auth/callback

# === AUTH-SYSTEM CONNECTION ===
AUTH_API_URL=http://localhost:3001/api # API URL used for registration

# === FRONTEND-LOGIN-PROXY MODE CONFIG ===
# For Frontend-Login-Proxy mode, you typically only need:
# - AUTH_SYSTEM_URL (for redirects)
# - ALLOWED_RETURN_URLS (for schema detection)
# 
# CLIENT_SECRET is only needed for:
# - API-Auth-Server mode
# - Administrative operations
# - Backend integrations

# === USAGE INSTRUCTIONS ===
# 1. Copy relevant values to your main .env file
# 2. For Frontend-Login-Proxy mode, you need:
#    - AUTH_SYSTEM_URL (from your .env, usually http://localhost:3000 for frontend)
#    - The return URLs should match your actual app URLs
# 
# 3. For API mode, you additionally need:
#    - CLIENT_ID
#    - CLIENT_SECRET
#    - AUTH_API_URL (from this file or your .env)

# === IMPORTANT SECURITY NOTES ===
# - Never commit CLIENT_SECRET to version control
# - Store CLIENT_SECRET securely in production
# - Use environment variables or secure config management
# - CLIENT_SECRET is only used for server-to-server communication
